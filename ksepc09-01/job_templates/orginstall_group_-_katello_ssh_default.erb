<%#
name: orgInstall Group - Katello SSH Default
snippet: false
model: JobTemplate
job_category: Katello
description_format: Install package group(s) %{package}
foreign_input_sets:
- exclude: action
  include_all: true
  template: orgInstall Group - Katello SSH Default
provider_type: SSH
kind: job_template
organizations:
- Default Organization
- KSE
locations:
- Default Location
- lpz
%>

<%= render_template('Package Action - SSH Default', :action => 'group install') %>